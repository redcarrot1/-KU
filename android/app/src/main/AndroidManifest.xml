<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".VolunteerKUApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VolunteerKU"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.NicknameActivity" />
        <activity android:name=".activity.MajorChoiceActivity" />
        <activity android:name=".activity.IntroductionActivity" />
        <activity android:name=".activity.EmailCertifyActivity" />
        <activity android:name=".activity.PasswordActivity" />

        <activity android:name=".activity.SuccessChangePasswordActivity" />
        <activity android:name=".activity.SuccessSignupActivity" />
        <activity android:name=".activity.ListActivity" />
        <activity android:name=".activity.Register2Activity" />
        <activity android:name=".activity.RegisterActivity" />

        <activity android:name=".activity.PasswordChangeActivity" />
        <activity android:name=".activity.PasswordChangeCertifyActivity" />

        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>